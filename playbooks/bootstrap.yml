---
- name: Configure Altair infrastructure (managed)
  hosts: servers
  become: true
  remote_user: "{{ ssh_login_user }}"
  vars:
    bootstrap_mode: managed
  roles:
    - base
    - docker
    - users
    - stacks
    - role: apps
      vars:
        apps_selected: [npm]
    - security
