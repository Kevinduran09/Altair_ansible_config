- name: Configuracion base del servidor (modo managed)
  hosts: servers
  become: true
  remote_user: "{{ ssh_login_user }}"
  vars:
    bootstrap_mode: managed

  roles:
    - base
    - docker
    - user-management
    - stacks
    - npm
    - security
