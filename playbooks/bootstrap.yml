- name: Configuracion inicial de ALTAIR
  hosts: altair_sv
  become: true
  vars_files:
    - ../group_vars/all.yml

  roles:
    - base
    - docker
    - user-management
    - security
    - stacks
    - npm
