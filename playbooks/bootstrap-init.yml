---
- name: Configure Altair infrastructure (init)
  hosts: servers
  become: true
  remote_user: "{{ bootstrap_init_user }}"
  vars:
    bootstrap_mode: init
  roles:
    - base
    - docker
    - users
    - stacks
    - role: apps
      vars:
        apps_selected: [npm]
    - security
