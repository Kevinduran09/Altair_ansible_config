all:
  children:
    servers:
      vars:
        ansible_port: "{{ ssh_port }}"
        ansible_host: "{{ server_ip }}"
        ansible_ssh_private_key_file: "{{ ssh_private_key_file | default('~/.ssh/id_ed25519_kumadev') }}"
      hosts:
        target: {}
